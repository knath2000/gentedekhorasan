{
  "compilerOptions": {
    "target": "esnext", // Keep target as esnext for modern syntax, CommonJS is for module system
    "module": "CommonJS", // Change to CommonJS for Vercel API routes
    "lib": [
      "esnext",
      "dom"
    ],
    "allowJs": true,
    "jsx": "react-native",
    "noEmit": false, // Allow TypeScript to emit files; Vercel will handle output for API routes
    "strict": true,
    "moduleResolution": "node", // Change to "node" for CommonJS compatibility
    "resolveJsonModule": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "paths": {
      "@/*": [
        "./*"
      ]
    }
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    ".expo/types/**/*.ts",
    "expo-env.d.ts"
  ],
  "extends": "expo/tsconfig.base"
}
